(class Math()
	ghost obj c: Int32:=0;
	ghost const x: Int32:=9;
	//invariant 5>0
	claim@c,x
	public proc add(in par: Int32)
	pre c>8
	post c>10
	post x<1
	takes x
	gives c
	(thread(*t0*) claim@c claim@x
		ghost const a: Int32:=9;
		assume c>9
		assert a<20
	thread) 
class)