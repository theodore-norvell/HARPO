package boogieBackEnd
import frontEnd.AST._
private class MethCodeGen(dlNd: DeclNd) {
  
  def getMethDeclCode(acc: Access, name: String, params: List[ParamDeclNd], preCnds: List[PreCndNd], postCnds: List[PostCndNd], givesPers: List[GivesPerNd], takesPers: List[TakesPerNd], borrowsPers: List[BorrowsPerNd]): String = {
   return ""
  }

  def getMethImplementationCode(): String = {
   return ""
  }

}